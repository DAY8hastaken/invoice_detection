import "../globals.css";
import SharedLayout from "../components/SharedLayout";
export const metadata = { title: "Help & Support · FinSight" };
export default function HelpLayout({ children }) {
  return <SharedLayout>{children}</SharedLayout>;
}
